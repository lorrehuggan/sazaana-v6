const config = {
  plugins: {
    autoprefixer: {},
    'postcss-nested': {},
    'postcss-preset-env': {
      stage: 3,
    },
    'postcss-easing-gradients': {},
    cssnano: {},
  },
};

module.exports = config;
