const config = {
  plugins: {
    'postcss-nested': {},
    autoprefixer: {},
    'postcss-preset-env': {
      stage: 3,
    },
    'postcss-easing-gradients': {},
    cssnano: {},
  },
};

module.exports = config;
