const config = {
  plugins: {
    'postcss-nested': {},
    'postcss-preset-env': {
      stage: 3,
    },
    'postcss-easing-gradients': {},
    autoprefixer: {},
    cssnano: {},
  },
};

module.exports = config;
